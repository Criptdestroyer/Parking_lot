#!/usr/bin/env bash
args=${1:-"iteractive"}

if [ $args = "iteractive" ]; then
    java -jar target/Parking_lot-1.0-SNAPSHOT.jar $args
    
else
    if [ -s $args ];then
        java -jar target/Parking_lot-1.0-SNAPSHOT.jar $args
        
    else
        echo "invalid filename"
        exit
    fi
fi